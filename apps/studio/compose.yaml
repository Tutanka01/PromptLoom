services:
  studio:
    build:
      context: ../..
      dockerfile: apps/studio/Dockerfile
    ports:
      - "${STUDIO_PORT:-3000}:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
